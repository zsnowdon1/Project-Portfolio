export interface SportsTypes {
  genre: string;
  selected: boolean;
}

export interface FinanceTypes {
  genre: string;
  selected: boolean;
}

export enum Sports {
  Basketball,
  Football,
  Soccer,
  Golf,
  Baseball,
}

export enum Finances {
  Stocks,
  HedgeFunds
}